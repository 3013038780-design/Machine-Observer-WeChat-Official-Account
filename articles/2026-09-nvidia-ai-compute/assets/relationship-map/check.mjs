import fs from 'node:fs/promises';import path from 'node:path';import {pathToFileURL} from 'node:url';
const {chromium}=await import('/Users/olivialee/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright-core/index.mjs');const b=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});const p=await b.newPage();await p.goto(pathToFileURL(path.join(import.meta.dirname,'full.svg')).href);await p.evaluate(()=>document.fonts.ready);const nodes=JSON.parse(await fs.readFile(path.join(import.meta.dirname,'nodes.json'),'utf8'));
const results=await p.evaluate(nodes=>nodes.map(n=>{const texts=[...document.querySelectorAll('text')];const title=texts.find(t=>t.querySelector('tspan')?.textContent===n.title);const body=n.lines.length?texts.find(t=>[...t.querySelectorAll('tspan')].map(s=>s.textContent).join('|')===n.lines.join('|')):null;return {id:n.id,missing:!title,overflow:[title,body].filter(Boolean).map(t=>{const r=t.getBBox();return r.x<n.x+4||r.x+r.width>n.x+n.w-4||r.y<n.y||r.y+r.height>n.y+n.h-2}).some(Boolean)};}),nodes);
const failed=results.filter(x=>x.missing||x.overflow);await fs.writeFile(path.join(import.meta.dirname,'visual-check.json'),JSON.stringify({nodesChecked:results.length,failed},null,2)+'\n');await b.close();if(failed.length)throw Error(JSON.stringify(failed));console.log('All '+results.length+' node text blocks fit');
